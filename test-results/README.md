# Test Results Directory

This directory contains analysis results saved for testing and debugging purposes.

## 📁 File Structure

Each analysis generates a matching pair of files:

### 📄 `analysis-{timestamp}.json`
Complete analysis result containing:
- **capturedText**: First 1000 characters + "..." (truncated for file size)
- **tempFile**: Path to the full captured text file
- **analysisFile**: Path to this JSON file
- **htmlFile**: Path to the HTML reconstruction file
- **stats**: Word count, text length, screen size
- **docType**: Detected document type (email, webpage, terminal, etc.)
- **structures**: Tables, navbars, headers, lists, grids found
- **zones**: Layout zones (header, sidebar, main, footer)
- **elements**: Individual UI elements with positions
- **reconstruction**: HTML reconstruction metadata
- **confidence**: AI confidence score (0-1)
- **timestamp**: Unix timestamp

### 🌐 `analysis-{timestamp}.html`
Visual HTML reconstruction of the captured screen:
- **Rendered layout** with proper positioning
- **Zone-based styling** (header, sidebar, main content)
- **Table rendering** with proper structure
- **Navigation bars** and UI elements
- **Responsive design** matching original screen size

### 📝 `capture-{timestamp}.txt` (in temp directory)
Full captured text (up to 100K characters) - see `tempFile` path in JSON

## 🔍 Testing Usage

### 1. **Inspect Analysis Quality**
```bash
# View latest analysis
ls -la *.json | tail -1
cat $(ls -t *.json | head -1) | jq .docType

# Check confidence scores
cat analysis-*.json | jq '.confidence'
```

### 2. **Visual Layout Testing**
```bash
# Open latest HTML reconstruction
open $(ls -t *.html | head -1)

# Compare different captures
ls -la *.html
```

### 3. **Debug Analysis Issues**
```bash
# Find failed analyses
grep -l '"success":false' *.json

# Check structure detection
cat analysis-*.json | jq '.structures'
```

## 🧹 Cleanup

Files are **not automatically cleaned** (unlike temp files) so you can:
- **Keep** for debugging and testing
- **Delete** when done: `rm -rf test-results/*`

## 📊 File Size Guidelines

- **JSON files**: ~5-50KB (truncated text)
- **HTML files**: ~10-200KB (includes CSS)
- **Text files**: Up to 100KB (full capture)

## 🎯 Testing Scenarios

Use these files to test:
- ✅ Document type detection accuracy
- ✅ Layout zone identification
- ✅ Table/form structure extraction
- ✅ Visual HTML reconstruction
- ✅ Performance benchmarks
- ✅ Error handling edge cases

---

**Generated by:** NutJS Screen Intelligence Analyzer
**Location:** `mcp-services/screen-intelligence-service/test-results/`
